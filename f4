aliaslist_clear          (struct AliasList *al)
attr_color_clear         (struct AttrColor *ac)
attr_color_list_clear    (struct AttrColorList *acl)
cert_array_clear         (struct CertArray *carr)
cid_map_list_clear       (struct CidMapList *cid_map_list)
maildirarray_clear       (struct MdEmailArray *mda)
mutt_addrlist_clear      (struct AddressList *al)
mutt_grouplist_clear     (struct GroupList *gl)
mutt_list_clear          (struct ListHead *h)
neomutt_mailboxlist_clear(struct MailboxList *ml)
regex_color_clear        (struct RegexColor *rcol)
regex_color_list_clear   (struct RegexColorList *rcl)
